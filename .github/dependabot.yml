version: 2

updates:
  # ================================
  # 📦 npm (VSCode Extension本体)
  # ================================
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "03:00"
      timezone: "Asia/Tokyo"

    open-pull-requests-limit: 5

    versioning-strategy: auto

    commit-message:
      prefix: "deps"
      include: "scope"

    labels:
      - "dependencies"
      - "npm"

    reviewers:
      - "long-910"

    assignees:
      - "long-910"

    groups:
      # minor/patch は1つのPRにまとめる
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"
      # major も1つのPRにまとめる（ignore 対象外のもの）
      major:
        update-types:
          - "major"

    ignore:
      # VSCode API — extension の engines.vscode に合わせて手動管理
      - dependency-name: "@types/vscode"
        update-types: ["version-update:semver-major"]
      # Node.js 型定義 — VSCode 内蔵 Node バージョンに合わせて手動管理
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      # ESLint major — 破壊的変更を含むため手動でマイグレーション
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@typescript-eslint/*"
        update-types: ["version-update:semver-major"]

  # ================================
  # ⚙ GitHub Actions
  # ================================
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "03:00"
      timezone: "Asia/Tokyo"

    open-pull-requests-limit: 3

    commit-message:
      prefix: "ci"
      include: "scope"

    labels:
      - "dependencies"
      - "github-actions"

    reviewers:
      - "long-910"

    assignees:
      - "long-910"

    groups:
      # minor/patch と major をそれぞれ1つのPRにまとめる
      actions-minor-and-patch:
        update-types:
          - "minor"
          - "patch"
      actions-major:
        update-types:
          - "major"
